#ifndef SETTINGS_H
#define SETTINGS_H

// Include the new configuration system
#include "Config.h"

// The old compile-time constants are now replaced by the conf:: namespace functions
// defined in Config.h. All existing code using conf::WIDTH, conf::HEIGHT, etc.
// will continue to work without modification.

#endif
